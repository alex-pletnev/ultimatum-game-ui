# Dev-defaults для локального backend'а ultimatum-game.
# Для персональных overrides — создай .env.local (см. .env.example).
# Публичные URL'ы, не секреты — можно коммитить.

VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_WS_URL=ws://localhost:8080/api/v1/ws
